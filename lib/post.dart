//class utk post
class Post {
  int? index;
  int? albumid;
  int id;
  String title;
  String url;
  String thumbnailUrl;

  Post(this.index, this.albumid, this.id, this.title, this.url, this.thumbnailUrl);
}
